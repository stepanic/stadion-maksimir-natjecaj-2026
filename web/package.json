{
  "name": "stadion-maksimir-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/prerender.ts && node scripts/prerender.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name stadion-maksimir --branch main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name stadion-maksimir"
  },
  "dependencies": {
    "@semaphore-protocol/core": "^4.14.3",
    "@supabase/supabase-js": "^2.117.2",
    "highlight.js": "^11.10.0",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0",
    "mermaid": "^11.4.1"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "happy-dom": "^20.14.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
